/**
 * This file was automatically generated by typeweaver.
 * DO NOT EDIT. Instead, modify the source definition file and generate again.
 *
 * @generated by @rexeus/typeweaver
 */

import { findReservedPathParameter, ReservedPathParameterError } from "@rexeus/typeweaver-core";
import type {
  HttpMethod,
  IHttpResponse,
  IRequestValidator,
  IValidatedHttpRequest,
  IResponseValidator,
  ITypedHttpResponse,
  RequestValidationError,
  ResponseValidationError,
} from "@rexeus/typeweaver-core";
import {
  AmbiguousPathSegmentError,
  ConflictingPathParameterNameError,
  DuplicateRouteRegistrationError,
} from "./errors/index.js";
import type { ErasedRequestHandler } from "./RequestHandler.js";
import type { ServerContext } from "./ServerContext.js";

/**
 * Metadata about a matched route, available in middleware and handlers via `ctx.route`.
 */
export type RouteMetadata = {
  readonly operationId: string;
  readonly method: HttpMethod;
  readonly path: string;
};

/**
 * A registered route with its method, path pattern, validator, and handler.
 */
export type RouteDefinition = {
  readonly operationId: string;
  readonly method: HttpMethod;
  readonly path: string;
  readonly requestValidator: IRequestValidator<IValidatedHttpRequest>;
  readonly responseValidator: IResponseValidator;
  readonly handler: ErasedRequestHandler;
  /** Reference to the router config for error handling. */
  readonly routerConfig: RouterErrorConfig;
};

/**
 * Error handling configuration associated with a router.
 */
export type RouterErrorConfig = {
  readonly validateRequests: boolean;
  readonly validateResponses: boolean;
  readonly handleHttpResponseErrors: HttpResponseErrorHandler | boolean;
  readonly handleRequestValidationErrors: RequestValidationErrorHandler | boolean;
  readonly handleResponseValidationErrors: ResponseValidationErrorHandler | boolean;
  readonly handleUnknownErrors: UnknownErrorHandler | boolean;
};

/**
 * Handles HTTP response errors thrown by request handlers.
 * The error parameter is a typed HTTP response object (thrown via `throw { type, statusCode, ... }`).
 */
export type HttpResponseErrorHandler = (
  error: ITypedHttpResponse,
  ctx: ServerContext,
) => Promise<IHttpResponse> | IHttpResponse;

/**
 * Handles request validation errors.
 */
export type RequestValidationErrorHandler = (
  error: RequestValidationError,
  ctx: ServerContext,
) => Promise<IHttpResponse> | IHttpResponse;

/**
 * Handles response validation errors.
 * Called when a handler returns a response that does not match the expected schema.
 */
export type ResponseValidationErrorHandler = (
  error: ResponseValidationError,
  response: IHttpResponse,
  ctx: ServerContext,
) => Promise<IHttpResponse> | IHttpResponse;

/**
 * Handles any unknown errors not caught by other handlers.
 */
export type UnknownErrorHandler = (
  error: unknown,
  ctx: ServerContext,
) => Promise<IHttpResponse> | IHttpResponse;

/**
 * Result of a successful route match.
 */
export type RouteMatch = {
  readonly route: RouteDefinition;
  readonly params: Record<string, string>;
};

type StaticSegmentToken = { readonly kind: "static"; readonly value: string };

type ParamSegmentToken = { readonly kind: "param"; readonly name: string };

type SegmentToken = StaticSegmentToken | ParamSegmentToken;

/**
 * A parsed path segment: literal text interleaved with canonical `:name`
 * placeholders. `shape` identifies the literal structure independently of the
 * placeholder names so ambiguous renames can be detected at registration.
 */
type SegmentPattern = {
  readonly tokens: readonly SegmentToken[];
  readonly parameterNames: readonly string[];
  readonly staticLength: number;
  readonly shape: string;
};

type DynamicSegmentChild = {
  readonly pattern: SegmentPattern;
  readonly node: RadixNode;
};

/**
 * A node in the radix tree.
 *
 * Each node represents a single path segment.
 * Static children are stored in a `Map` keyed by the exact segment string.
 * Dynamic (placeholder-bearing) children are keyed by their literal shape.
 * Leaf nodes store a `Map` of HTTP method → route definition.
 */
type RadixNode = {
  readonly staticChildren: Map<string, RadixNode>;
  readonly dynamicChildren: Map<string, DynamicSegmentChild>;
  readonly methods: Map<string, RouteDefinition>;
};

/**
 * High-performance radix tree router with path parameter support.
 *
 * Routes are stored in a tree structure where each level corresponds
 * to a path segment. This gives O(d) lookup time where d is the depth
 * (number of segments) of the URL — independent of the total number
 * of registered routes.
 *
 * Supports:
 * - Static paths: `/accounts`
 * - Named parameters: `/todos/:todoId`
 * - Multiple parameters: `/todos/:todoId/subtodos/:subtodoId`
 * - Embedded parameters within one segment: `/files/:fileId.:format`
 * - Automatic HEAD → GET fallback (per HTTP spec)
 * - 405 Method Not Allowed detection with `Allow` header
 */
export class Router {
  private readonly root: RadixNode = Router.createNode();

  private static assertPathHasNoReservedParameter(path: string): void {
    const reservedPathParameter = findReservedPathParameter(path);
    if (reservedPathParameter !== undefined) {
      throw new ReservedPathParameterError(path, reservedPathParameter);
    }
  }

  private static normalizeDefinition(definition: RouteDefinition, method: string): RouteDefinition {
    return definition.method === method
      ? definition
      : { ...definition, method: method as HttpMethod };
  }

  /**
   * Register a route in the radix tree.
   */
  public add(definition: RouteDefinition): void {
    Router.assertPathHasNoReservedParameter(definition.path);

    const method = definition.method.toUpperCase();
    const normalizedDefinition = Router.normalizeDefinition(definition, method);
    const segments = Router.toSegments(definition.path);

    let current = this.root;

    for (const segment of segments) {
      current = Router.descend(
        current,
        definition.path,
        Router.parseSegment(segment, definition.path),
      );
    }

    if (current.methods.has(method)) {
      throw new DuplicateRouteRegistrationError(method, definition.path);
    }

    current.methods.set(method, normalizedDefinition);
  }

  private static descend(node: RadixNode, path: string, pattern: SegmentPattern): RadixNode {
    if (pattern.tokens.every((token) => token.kind === "static")) {
      const value = pattern.tokens
        .map((token) => (token.kind === "static" ? token.value : ""))
        .join("");
      let child = node.staticChildren.get(value);

      if (child === undefined) {
        child = Router.createNode();
        node.staticChildren.set(value, child);
      }

      return child;
    }

    const existing = node.dynamicChildren.get(pattern.shape);

    if (existing !== undefined) {
      if (!Router.sameParameterNames(existing.pattern, pattern)) {
        throw new ConflictingPathParameterNameError(
          path,
          existing.pattern.parameterNames.join(", "),
          pattern.parameterNames.join(", "),
        );
      }

      return existing.node;
    }

    const child = Router.createNode();
    node.dynamicChildren.set(pattern.shape, { pattern, node: child });
    return child;
  }

  private static sameParameterNames(left: SegmentPattern, right: SegmentPattern): boolean {
    return (
      left.parameterNames.length === right.parameterNames.length &&
      left.parameterNames.every((name, index) => name === right.parameterNames[index])
    );
  }

  /**
   * Reads a canonical `:name` placeholder starting at `start`. A `:` without a
   * following `[A-Za-z0-9_]` name character is not a placeholder.
   */
  private static readPlaceholder(
    segment: string,
    start: number,
  ): { readonly name: string; readonly length: number } | undefined {
    if (segment.charAt(start) !== ":") return undefined;

    const name = Router.readPlaceholderName(segment, start + 1);
    if (name === undefined) return undefined;

    return { name, length: 1 + name.length };
  }

  /**
   * Parses one path segment into literal and canonical `:name` placeholder
   * tokens. A `:` without a following `[A-Za-z0-9_]` name character stays
   * literal. Adjacent placeholders without a static separator are ambiguous and
   * rejected.
   */
  private static parseSegment(segment: string, path: string): SegmentPattern {
    const tokens: SegmentToken[] = [];
    let staticBuffer = "";
    let index = 0;

    const flushStatic = (): void => {
      if (staticBuffer.length > 0) {
        tokens.push({ kind: "static", value: staticBuffer });
        staticBuffer = "";
      }
    };

    while (index < segment.length) {
      const placeholder = Router.readPlaceholder(segment, index);
      if (placeholder !== undefined) {
        flushStatic();
        if (tokens.at(-1)?.kind === "param") {
          throw new AmbiguousPathSegmentError(path, segment);
        }
        tokens.push({ kind: "param", name: placeholder.name });
        index += placeholder.length;
        continue;
      }

      staticBuffer += segment.charAt(index);
      index += 1;
    }

    flushStatic();

    const parameterNames = tokens
      .filter((token): token is ParamSegmentToken => token.kind === "param")
      .map((token) => token.name);
    const staticLength = tokens.reduce(
      (total, token) => total + (token.kind === "static" ? token.value.length : 0),
      0,
    );
    const shape = JSON.stringify(
      tokens.map((token) => (token.kind === "static" ? ["s", token.value] : ["p"])),
    );

    return { tokens, parameterNames, staticLength, shape };
  }

  private static readPlaceholderName(segment: string, start: number): string | undefined {
    let end = start;

    while (end < segment.length) {
      const code = segment.charCodeAt(end);
      const isNameCharacter =
        (code >= 0x30 && code <= 0x39) ||
        (code >= 0x41 && code <= 0x5a) ||
        (code >= 0x61 && code <= 0x7a) ||
        code === 0x5f;

      if (!isNameCharacter) break;

      end += 1;
    }

    return end > start ? segment.slice(start, end) : undefined;
  }

  /**
   * Find a matching route for the given method and path.
   *
   * Traverses the radix tree in O(d) time where d is the number
   * of path segments. Static segments are matched first (exact match),
   * then parameterized segments are tried as fallback.
   *
   * For HEAD requests, automatically falls back to the GET handler
   * if no explicit HEAD handler is registered (per HTTP spec).
   *
   * @returns The matched route with extracted path parameters, or `undefined` if no match.
   */
  public match(method: string, path: string): RouteMatch | undefined {
    const upperMethod = method.toUpperCase();
    const segments = Router.toSegments(path);
    const params: Record<string, string> = Object.create(null) as Record<string, string>;

    const node = this.traverse(this.root, segments, 0, params);
    if (!node) return undefined;

    const definition =
      node.methods.get(upperMethod) ??
      (upperMethod === "HEAD" ? node.methods.get("GET") : undefined);

    if (!definition) return undefined;

    return { route: definition, params };
  }

  /**
   * Find a matching node for the given path, regardless of HTTP method.
   *
   * Used to distinguish "path not found" (404) from "method not allowed" (405).
   *
   * @returns The allowed methods for this path, or `undefined` if the path doesn't exist.
   */
  public matchPath(path: string): { allowedMethods: string[] } | undefined {
    const segments = Router.toSegments(path);
    const params: Record<string, string> = Object.create(null) as Record<string, string>;

    const node = this.traverse(this.root, segments, 0, params);
    if (!node || node.methods.size === 0) return undefined;

    const methods = Array.from(node.methods.keys());
    if (methods.includes("GET") && !methods.includes("HEAD")) {
      methods.push("HEAD");
    }

    return { allowedMethods: methods.sort() };
  }

  /**
   * Recursively traverse the radix tree to find a matching node.
   *
   * Static children are tried first (exact match). Dynamic children are then
   * tried most-specific-first (longer literal text wins), so an embedded
   * pattern such as `:fileId.:format` is preferred over a bare `:fileId`
   * fallback. Captured params are removed again when a branch backtracks.
   *
   * Path parameters are URL-decoded during extraction.
   */
  private traverse(
    node: RadixNode,
    segments: string[],
    index: number,
    params: Record<string, string>,
  ): RadixNode | undefined {
    // All segments consumed — this node is the match candidate
    if (index === segments.length) {
      return node;
    }

    const segment = segments[index] ?? "";

    // 1. Try static child first (higher priority)
    const staticChild = node.staticChildren.get(segment);
    if (staticChild) {
      const result = this.traverse(staticChild, segments, index + 1, params);
      if (result && result.methods.size > 0) return result;
    }

    // 2. Try dynamic children, most specific first
    for (const candidate of Router.sortedDynamicChildren(node)) {
      const captured = Router.matchSegment(candidate.pattern, segment);
      if (captured === undefined) continue;

      const capturedNames = Router.writeCaptured(captured, params);
      const result = this.traverse(candidate.node, segments, index + 1, params);
      if (result && result.methods.size > 0) return result;

      Router.clearCaptured(capturedNames, params);
    }

    return undefined;
  }

  private static writeCaptured(
    captured: Record<string, string>,
    params: Record<string, string>,
  ): string[] {
    const names = Object.keys(captured);

    for (const name of names) {
      params[name] = captured[name] ?? "";
    }

    return names;
  }

  private static clearCaptured(names: readonly string[], params: Record<string, string>): void {
    for (const name of names) {
      delete params[name];
    }
  }

  private static sortedDynamicChildren(node: RadixNode): DynamicSegmentChild[] {
    return [...node.dynamicChildren.values()].sort(
      (left, right) =>
        right.pattern.staticLength - left.pattern.staticLength ||
        left.pattern.shape.localeCompare(right.pattern.shape),
    );
  }

  /**
   * Matches one request segment against a parsed segment pattern, returning the
   * captured (and URL-decoded) placeholder values, or `undefined` when the
   * segment does not fit.
   */
  private static matchSegment(
    pattern: SegmentPattern,
    segment: string,
  ): Record<string, string> | undefined {
    const captured: Record<string, string> = Object.create(null) as Record<string, string>;
    let cursor = 0;

    for (let index = 0; index < pattern.tokens.length; index += 1) {
      const token = pattern.tokens[index];
      if (token === undefined) return undefined;

      if (token.kind === "static") {
        if (!segment.startsWith(token.value, cursor)) return undefined;
        cursor += token.value.length;
        continue;
      }

      const matched = Router.matchParamToken(pattern.tokens[index + 1], segment, cursor);

      if (matched === undefined) return undefined;

      captured[token.name] = matched.value;
      cursor = matched.nextCursor;
    }

    return cursor === segment.length ? captured : undefined;
  }

  private static matchParamToken(
    next: SegmentToken | undefined,
    segment: string,
    cursor: number,
  ): { readonly value: string; readonly nextCursor: number } | undefined {
    if (next === undefined) {
      if (cursor >= segment.length) return undefined;

      return {
        value: Router.decodePathSegment(segment.slice(cursor)),
        nextCursor: segment.length,
      };
    }

    if (next.kind !== "static") return undefined;

    const end = segment.indexOf(next.value, cursor);
    if (end === -1 || end === cursor) return undefined;

    return {
      value: Router.decodePathSegment(segment.slice(cursor, end)),
      nextCursor: end,
    };
  }

  private static createNode(): RadixNode {
    return {
      staticChildren: new Map(),
      dynamicChildren: new Map(),
      methods: new Map(),
    };
  }

  /**
   * Decodes a URL-encoded path segment while guarding against path traversal.
   *
   * Encoded dot-segments like `%2e%2e` would decode to `..`, which could
   * enable directory traversal if a downstream handler builds file paths
   * from params. Returning the raw segment neutralises this vector.
   */
  private static decodePathSegment(segment: string): string {
    try {
      const decoded = decodeURIComponent(segment);
      if (decoded === ".." || decoded === "." || decoded.includes("/") || decoded.includes("\\")) {
        return segment;
      }
      return decoded;
    } catch {
      return segment;
    }
  }

  private static toSegments(path: string): string[] {
    return path.split("/").filter((s) => s.length > 0);
  }
}
