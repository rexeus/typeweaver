/**
 * This file was automatically generated by TypeWeaver.
 * DO NOT EDIT. Modify the source contract and generate again.
 *
 * @generated by @rexeus/typeweaver-command
 */

import { HttpMethod } from "@rexeus/typeweaver-core";
import { defineGeneratedCommand } from "../../lib/command/index.js";
import { TodoClient } from "../../todo/TodoClient.js";
import { DeleteTodoRequestCommand } from "../../todo/DeleteTodoRequestCommand.js";
import { DeleteTodoRequestValidator } from "../../todo/DeleteTodoRequestValidator.js";

const requestValidator = new DeleteTodoRequestValidator();

export const DeleteTodoCommand = defineGeneratedCommand({
  name: "delete-todo",
  operationId: "DeleteTodo",
  summary: "Delete todo",
  inputs: [
    {
      flag: "path-todo-id",
      key: "todoId",
      target: "path",
      required: true,
      multiple: false,
    },
    {
      flag: "header-x-single-value",
      key: "X-Single-Value",
      target: "header",
      required: false,
      multiple: false,
    },
    {
      flag: "header-x-multi-value",
      key: "X-Multi-Value",
      target: "header",
      required: false,
      multiple: true,
    },
  ],
  headerDefaults: {
    Accept: "application/json",
  },
  security: {
    requirements: [["bearerAuth"]],
    schemes: [
      {
        name: "bearerAuth",
        flag: "auth-bearer-auth",
        kind: "http",
        scheme: "bearer",
      },
    ],
  },
  hasBody: false,

  execute: async (context) => {
    const validatedRequest = requestValidator.validate({
      method: HttpMethod.DELETE,
      path: "/todos/:todoId",
      header: {
        ...{ Accept: "application/json" },
        ...context.defaultHeaders,
        ...context.request.header,
      },
      param: context.request.param,
    });
    const client = new TodoClient({
      baseUrl: context.baseUrl,
      defaultHeaders: context.defaultHeaders,
      defaultQuery: context.defaultQuery,
      signal: context.signal,
    });
    return client.send(
      new DeleteTodoRequestCommand({
        header: validatedRequest.header,
        param: validatedRequest.param,
      }),
    );
  },
});
