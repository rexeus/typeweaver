/**
 * This file was automatically generated by TypeWeaver.
 * DO NOT EDIT. Modify the source contract and generate again.
 *
 * @generated by @rexeus/typeweaver-command
 */

import { HttpMethod } from "@rexeus/typeweaver-core";
import { defineGeneratedCommand } from "../../lib/command/index.js";
import { TodoClient } from "../../todo/TodoClient.js";
import { CreateTodoRequestCommand } from "../../todo/CreateTodoRequestCommand.js";
import { CreateTodoRequestValidator } from "../../todo/CreateTodoRequestValidator.js";

const requestValidator = new CreateTodoRequestValidator();

export const CreateTodoCommand = defineGeneratedCommand({
  name: "create-todo",
  operationId: "CreateTodo",
  summary: "Create new todo",
  inputs: [
    {
      flag: "header-x-single-value",
      key: "X-Single-Value",
      target: "header",
      required: false,
      multiple: false,
    },
    {
      flag: "header-x-multi-value",
      key: "X-Multi-Value",
      target: "header",
      required: false,
      multiple: true,
    },
  ],
  headerDefaults: {
    Accept: "application/json",
    "Content-Type": "application/json",
  },
  security: {
    requirements: [["bearerAuth"]],
    schemes: [
      {
        name: "bearerAuth",
        flag: "auth-bearer-auth",
        kind: "http",
        scheme: "bearer",
      },
    ],
  },
  hasBody: true,
  bodyTransport: "json",
  execute: async (context) => {
    const validatedRequest = requestValidator.validate({
      method: HttpMethod.POST,
      path: "/todos",
      header: {
        ...{ Accept: "application/json", "Content-Type": "application/json" },
        ...context.defaultHeaders,
        ...context.request.header,
      },
      body: context.request.body,
    });
    const client = new TodoClient({
      baseUrl: context.baseUrl,
      defaultHeaders: context.defaultHeaders,
      defaultQuery: context.defaultQuery,
      signal: context.signal,
    });
    return client.send(
      new CreateTodoRequestCommand({
        header: validatedRequest.header,
        body: validatedRequest.body,
      }),
    );
  },
});
