/* eslint-disable */
/**
 * This file was automatically generated by typeweaver.
 * DO NOT EDIT. Instead, modify the source definition file and generate again.
 * 
 * @generated by @rexeus/typeweaver
 */

<% if (hasRequestSchema) { %>import { spec } from "<%= specPath %>";
<% } %>import {
  HttpMethod,
  type IRawHttpRequest,
  type SafeRequestValidationResult,
  RequestValidationError
} from "@rexeus/typeweaver-core";
import { <%= hasRequestSchema ? "getOperationDefinition, " : "" %>RequestValidator } from "../lib/types/index.js";
import type {
  I<%= pascalCaseOperationId %>Request<% if(body) { %>,
  I<%= pascalCaseOperationId %>RequestBody<% } %><% if(header) { %>,
  I<%= pascalCaseOperationId %>RequestHeader<% } %><% if(param) { %>,
  I<%= pascalCaseOperationId %>RequestParam<% } %><% if(query) { %>,
  I<%= pascalCaseOperationId %>RequestQuery<% } %>
} from "<%= requestFile %>";

<% if (hasRequestSchema) { %>const definition = getOperationDefinition(spec, "<%= resourceName %>", "<%= operationId %>");

<% } %>export class <%= pascalCaseOperationId %>RequestValidator extends RequestValidator<I<%= pascalCaseOperationId %>Request> {
    public safeValidate(request: IRawHttpRequest): SafeRequestValidationResult<I<%= pascalCaseOperationId %>Request> {
        const error = new RequestValidationError();

        <% if(body) { %>
        const bodySchema = this.requireRequestSchema(
          definition.request.body,
          "body"
        );
        const bodyResult = this.safeParseAs<I<%= pascalCaseOperationId %>RequestBody>(
          bodySchema,
          request.body
        );
        if (!bodyResult.success) {
          error.addBodyIssues(bodyResult.error.issues);
        }
        <% } %>

        <% if(header) { %>
        const headerSchema = this.requireRequestSchema(
          definition.request.header,
          "header"
        );
        const coercedHeader = this.coerceHeaderToSchema(
          request.header,
          headerSchema
        );
        const headerMultiplicityIssues = this.findMultiplicityIssues(
          request.header,
          headerSchema,
          false
        );
        if (headerMultiplicityIssues.length > 0) {
          error.addHeaderIssues(headerMultiplicityIssues);
        }
        const headerResult = this.safeParseAs<I<%= pascalCaseOperationId %>RequestHeader>(
          headerSchema,
          coercedHeader
        );
        if (!headerResult.success) {
          error.addHeaderIssues(headerResult.error.issues);
        }
        <% } %>

        <% if(param) { %>
        const paramSchema = this.requireRequestSchema(
          definition.request.param,
          "param"
        );
        const paramResult = this.safeParseAs<I<%= pascalCaseOperationId %>RequestParam>(
          paramSchema,
          request.param
        );
        if (!paramResult.success) {
          error.addPathParamIssues(paramResult.error.issues);
        }
        <% } %>

        <% if(query) { %>
        const querySchema = this.requireRequestSchema(
          definition.request.query,
          "query"
        );
        const coercedQuery = this.coerceQueryToSchema(
          request.query,
          querySchema
        );
        const queryMultiplicityIssues = this.findMultiplicityIssues(
          request.query,
          querySchema,
          true
        );
        if (queryMultiplicityIssues.length > 0) {
          error.addQueryIssues(queryMultiplicityIssues);
        }
        const queryResult = this.safeParseAs<I<%= pascalCaseOperationId %>RequestQuery>(
          querySchema,
          coercedQuery
        );
        if (!queryResult.success) {
          error.addQueryIssues(queryResult.error.issues);
        }
        <% } %>

        if (
          error.hasIssues()<% if(body) { %> ||
          !bodyResult.success<% } %><% if(header) { %> ||
          !headerResult.success<% } %><% if(param) { %> ||
          !paramResult.success<% } %><% if(query) { %> ||
          !queryResult.success<% } %>
        ) {
          return {
            isValid: false,
            error,
          };
        }

        return {
          isValid: true,
          data: {
            method: HttpMethod.<%= method %>,
            path: request.path,
            <% if(body) { %>body: bodyResult.data,<% } %>
            <% if(header) { %>header: headerResult.data,<% } %>
            <% if(param) { %>param: paramResult.data,<% } %>
            <% if(query) { %>query: queryResult.data,<% } %>
          },
        };
    }

    public validate(request: IRawHttpRequest): I<%= pascalCaseOperationId %>Request {
        const result = this.safeValidate(request);

        if (!result.isValid) {
          throw result.error;
        }

        return result.data;
    }
}
