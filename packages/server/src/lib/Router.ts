/**
 * This file was automatically generated by typeweaver.
 * DO NOT EDIT. Instead, modify the source definition file and generate again.
 *
 * @generated by @rexeus/typeweaver
 */

import { DuplicateRouteRegistrationError } from "./errors/index.js";
import {
  assertPathHasNoReservedParameter,
  createNode,
  descend,
  normalizeDefinition,
  parseSegment,
  toSegments,
} from "./routerInternals.js";
import { traverse } from "./routerMatching.js";
import type {
  HttpResponseErrorHandler,
  RequestValidationErrorHandler,
  ResponseValidationErrorHandler,
  RouteDefinition,
  RouteMatch,
  RouteMetadata,
  RouterErrorConfig,
  UnknownErrorHandler,
} from "./routerTypes.js";

export type {
  HttpResponseErrorHandler,
  RequestValidationErrorHandler,
  ResponseValidationErrorHandler,
  RouteDefinition,
  RouteMatch,
  RouteMetadata,
  RouterErrorConfig,
  UnknownErrorHandler,
};

export class Router {
  private readonly root = createNode();

  public add(definition: RouteDefinition): void {
    assertPathHasNoReservedParameter(definition.path);
    const method = definition.method.toUpperCase();
    const normalizedDefinition = normalizeDefinition(definition, method);
    let current = this.root;

    for (const segment of toSegments(definition.path)) {
      current = descend(
        current,
        definition.path,
        parseSegment(segment, definition.path)
      );
    }

    if (current.methods.has(method)) {
      throw new DuplicateRouteRegistrationError(method, definition.path);
    }
    current.methods.set(method, normalizedDefinition);
  }

  public match(method: string, path: string): RouteMatch | undefined {
    const upperMethod = method.toUpperCase();
    const params: Record<string, string> = Object.create(null) as Record<
      string,
      string
    >;
    const node = traverse(this.root, toSegments(path), 0, params);
    if (!node) return undefined;

    const definition =
      node.methods.get(upperMethod) ??
      (upperMethod === "HEAD" ? node.methods.get("GET") : undefined);
    return definition === undefined ? undefined : { route: definition, params };
  }

  public matchPath(path: string): { allowedMethods: string[] } | undefined {
    const params: Record<string, string> = Object.create(null) as Record<
      string,
      string
    >;
    const node = traverse(this.root, toSegments(path), 0, params);
    if (!node || node.methods.size === 0) return undefined;

    const methods = Array.from(node.methods.keys());
    if (methods.includes("GET") && !methods.includes("HEAD")) {
      methods.push("HEAD");
    }
    return { allowedMethods: methods.sort() };
  }
}
