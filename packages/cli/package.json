{
  "name": "@rexeus/typeweaver",
  "version": "0.0.0",
  "description": "Command-line interface for the TypeWeaver API framework",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json",
    "README.md"
  ],
  "bin": {
    "typeweaver": "dist/run-cli-with-tsx.js"
  },
  "scripts": {
    "cli": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit --project tsconfig.test.json",
    "format": "prettier --write .",
    "build": "pkgroll --clean-dist && cp -r ./src/generators/templates ./dist/templates",
    "prepack": "npm run build",
    "preversion": "npm run build",
    "test:bundle": "pnpm run build && node dist/run-cli-with-tsx.js generate --output test/output --input test/definition --plugins clients,aws-cdk",
    "test:gen": "tsx src/cli.ts generate --output test/output --input test/definition --plugins clients,aws-cdk"
  },
  "keywords": [
    "api",
    "spec",
    "definition",
    "typescript",
    "zod",
    "generator",
    "typeweaver"
  ],
  "author": "Dennis Wentzien <dw@rexeus.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rexeus/typeweaver.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/rexeus/typeweaver/issues"
  },
  "homepage": "https://github.com/rexeus/typeweaver#readme",
  "devDependencies": {
    "@rexeus/typeweaver-core": "workspace:*",
    "@rexeus/typeweaver-gen": "workspace:*",
    "@rexeus/typeweaver-types": "workspace:*",
    "@rexeus/typeweaver-clients": "workspace:*",
    "@rexeus/typeweaver-aws-cdk": "workspace:*",
    "axios": "^1.9.0",
    "case": "^1.6.3"
  },
  "peerDependencies": {
    "@rexeus/typeweaver-core": "*",
    "@rexeus/typeweaver-gen": "*",
    "@rexeus/typeweaver-types": "*",
    "@rexeus/typeweaver-clients": "*",
    "@rexeus/typeweaver-aws-cdk": "*"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "tsx": "^4.19.4"
  }
}
