/* eslint-disable */
/**
 * This file was automatically generated by typeweaver.
 * DO NOT EDIT. Instead, modify the source definition file and generate again.
 *
 * @generated by @rexeus/typeweaver
 */

import { ApiClient, type ApiClientProps } from "../lib/clients/index.js";
<% for (const operation of operations) { %>
import { <%= operation.pascalCaseOperationId %>RequestCommand } from "./<%= operation.pascalCaseOperationId %>RequestCommand.js";
import type { <%= operation.pascalCaseOperationId %>Response } from "<%= operation.responseFile %>";
<% } %>

export type <%= pascalCaseEntityName %>RequestCommands =
<% for (const operation of operations) { %>
    | <%= operation.pascalCaseOperationId %>RequestCommand
<% } %>;

export type <%= pascalCaseEntityName %>Responses =
<% for (const operation of operations) { %>
    | <%= operation.pascalCaseOperationId %>Response
<% } %>;


export class <%= pascalCaseEntityName %>Client extends ApiClient {
    public constructor(props: ApiClientProps) {
        super(props);
    }

    <% for (const operation of operations) { %>
    <% if (operation.jsDoc) { %><%- operation.jsDoc %>
    <% } %>public async send(command: <%= operation.pascalCaseOperationId %>RequestCommand): Promise<<%= operation.pascalCaseOperationId %>Response>;
    <% } %>
    public async send(command: <%= pascalCaseEntityName %>RequestCommands): Promise<<%= pascalCaseEntityName %>Responses> {
        const response = await this.execute(command);
        return command.processResponse(response);
    }
}
