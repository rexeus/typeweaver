---
"@rexeus/typeweaver": minor
---

Replace the placeholder `init` command with an atomic Todo project bootstrap, explicit target and
overwrite policy, dry-run planning, selectable JavaScript config formats, rollback on publication
failure, and stable human and JSON reports.
