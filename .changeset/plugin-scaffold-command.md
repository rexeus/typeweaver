---
"@rexeus/typeweaver": minor
---

Add a non-interactive `typeweaver add plugin` command that creates a deterministic, tested
third-party plugin starter in a new directory without overwriting user files.
