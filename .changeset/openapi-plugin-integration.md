---
"@rexeus/typeweaver-openapi": patch
"@rexeus/typeweaver": patch
"@rexeus/typeweaver-types": patch
---

Add the OpenAPI generator plugin so Typeweaver can emit OpenAPI 3.1.1 JSON documents during generation.
Fix the schema-less request validator template so generated validators handle routes without request schemas correctly.
