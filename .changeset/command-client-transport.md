---
"@rexeus/typeweaver-clients": minor
---

Add default request headers and query values plus external AbortSignal forwarding to generated
clients, and export the deterministic request-header default extractor for composed generators.
